######################################################
##                                                  ##
##  !!!! Autogenerated YAML file, do not edit !!!!  ##
##                                                  ##
##  Edit source in /src/github/workflows/ instead!  ##
##                                                  ##
######################################################
name: Build (cabal)

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
on:
  push:
    branches:
    - main
    - ci-*
    - release*
    paths: &trigger_path_list
    - '.github/workflows/cabal.yml'
    - 'dype.cabal'
    - 'src/Dayan/**'
  pull_request:
    paths: *trigger_path_list

defaults:
  run:
    shell: bash

jobs:
  auto-cancel:
    if: |
      !contains(github.event.head_commit.message, '[skip ci]')
      && !contains(github.event.head_commit.message, '[ci skip]')
      && !contains(github.event.head_commit.message, '[github skip]')
      && !contains(github.event.head_commit.message, '[skip github]')
    runs-on: ubuntu-latest

    steps:
    - uses: styfle/cancel-workflow-action@0.13.1
      with:
        access_token: ${{ github.token }}

  cabal:
    needs: auto-cancel

    timeout-minutes: 60

    name: Cabal ${{ matrix.description }}, ${{ matrix.ghc-ver }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04]
        description: [Linux]
        ghc-ver: [9.14.1, 9.12.4, 9.10.3, 9.8.4, 9.6.7, 9.4.8]
        include:
          # macOS with default flags
          - os: macos-26
            description: macOS
            ghc-ver: '9.14.1'
          # Windows with default flags
          - os: windows-2025
            description: Windows
            ghc-ver: '9.14.1'

    env:
      GHC_VER: ${{ matrix.ghc-ver || '9.14.1' }}
      FLAGS:   -O0

    steps:
    - uses: actions/checkout@v7

    - name: Switch off XDG directories for cabal (Linux)
      if: runner.os == 'Linux'
      run: |
        mkdir -p ~/.cabal

    - uses: clearnature/setup@main
      id: setup-haskell
      with:
        ghc-version:   ${{ env.GHC_VER }}
        cabal-version: latest
        cabal-update:  true

    - name: Install the ICU library (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install icu4c
        ICU4C=$(brew --prefix)/opt/icu4c
        echo "PKG_CONFIG_PATH=${ICU4C}/lib/pkgconfig" >> "${GITHUB_ENV}"

    - name: Install the ICU library (Windows)
      if: runner.os == 'Windows'
      run: |
        # shellcheck disable=SC2086
        stack exec -- pacman --noconfirm -S mingw-w64-x86_64-icu mingw-w64-x86_64-pkgconf mingw-w64-x86_64-zlib

    - name: Determine the ICU version
      run: |
        if [ "${{ runner.os }}" = "Windows" ]; then
          ICU_VER=$(stack exec -- pacman -Qi mingw-w64-x86_64-icu 2>/dev/null | grep Version | head -1 | sed 's/.*: //' | cut -d- -f1 || echo "0")
        else
          ICU_VER=$(pkg-config --modversion icu-i18n 2>/dev/null || echo "0")
        fi
        echo "ICU_VER=${ICU_VER}"
        echo "ICU_VER=${ICU_VER}" >> "${GITHUB_ENV}"

    - name: Configure the build plan
      run: |
        # shellcheck disable=SC2086
        cabal build ${FLAGS} --dry-run

    - name: Restore cache from approximate key
      uses: actions/cache/restore@v6
      id: cache
      with:
        path: &cache_path |
          ${{ steps.setup-haskell.outputs.cabal-store }}
        key: ${{ env.key }}-${{ hashFiles('**/plan.json') }}
        restore-keys: ${{ env.key }}
      env:
        key: cabal.yml-${{ runner.os }}-ghc-${{ steps.setup-haskell.outputs.ghc-version }}-cabal-${{ steps.setup-haskell.outputs.cabal-version }}-icu

    - name: Install dependencies
      run: |
        # shellcheck disable=SC2086
        cabal build ${FLAGS} --only-dependencies

    - name: Build dype
      run: |
        # shellcheck disable=SC2086
        cabal build ${FLAGS}

    - name: Run tests
      run: |
        # shellcheck disable=SC2086
        cabal test ${FLAGS}

    - name: Run benchmarks
      run: |
        # shellcheck disable=SC2086
        cabal bench ${FLAGS}

    - name: Save cache
      uses: actions/cache/save@v6
      if:   always() && steps.cache.outputs.cache-hit != 'true'
      with:
        key:  ${{ steps.cache.outputs.cache-primary-key }}
        path: *cache_path
